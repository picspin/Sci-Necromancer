# Cloudflare Pages 配置
# 仅用于前端静态文件部署

name = "sci-necromancer"
compatibility_date = "2024-01-01"

# Pages 配置 (通过 Cloudflare Dashboard 或 wrangler pages)
# 构建命令: npm run build
# 输出目录: dist
# 根目录: /

# 环境变量 (在 Cloudflare Dashboard 中配置)
# VITE_API_BASE_URL = https://your-vercel-backend.vercel.app

[site]
bucket = "./dist"
